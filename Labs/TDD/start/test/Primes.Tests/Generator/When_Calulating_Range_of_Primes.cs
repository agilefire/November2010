using NUnit.Framework;

namespace Primes.Generator
{
	[TestFixture]
	public class When_Calulating_Range_of_Primes
	{
		/*
		 * Create tests to verify ability to output 5 prime numbers in CSV format
		 * 
		 * Create tests to verify ability to output a count of primes evaluated
		 * thus far for every 10 lines of numbers
		 * 
		 */
	}
}